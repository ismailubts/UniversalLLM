apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    {{- include "universalllm.labels" . | nindent 4 }}
  name: {{ include "universalllm.fullname" . }}-config
data:
{{- range $key, $value := .Values.config }}
  {{ $key }}: "{{ $value }}"
{{- end }}