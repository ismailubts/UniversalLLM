apiVersion: v1
kind: Service
metadata:
  name: {{ include "universalllm.fullname" . }}
  labels:
    {{- include "universalllm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "universalllm.selectorLabels" . | nindent 4 }}
